<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ (API 31) ignore le drawable "launch_background" et gère
         lui-même l'écran de démarrage via la Splash Screen API : sans ces
         attributs, il encadre l'icône de lancement d'un halo blanc par
         défaut. On force ici le même bleu nuit que le fond du logo, et on
         supprime le halo (fond d'icône = même bleu), pour un écran uniforme
         sans bordure blanche visible. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
